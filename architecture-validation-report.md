# 基于 Qwen3.5-9B 的自回归文生图架构验证报告

## 摘要

本实验验证了一套基于 `Qwen3.5-9B + 离散视觉 Tokenizer + 自回归视觉解码器` 的文生图架构是否可以跑通，并分析其在极小数据集条件下的初步语义对齐能力。实验数据仅约 `826` 张图文对，因此本次工作不以生成高质量图像为目标，而以验证工程闭环、排查训练塌缩、观察最低层级语义响应为目标。结果表明，该架构在真实代码与真实 tokenizer 下可以完成从文本输入到图像输出的完整流程；同时，模型已经对 `red`、`yellow`、`green` 等颜色词表现出可观测的条件响应，但尚未学会稳定的对象结构生成。

## 1. 研究目标

- 验证自回归文生图完整链路是否成立。
- 确认真实视觉 tokenizer、训练脚本、推理脚本、VQ 解码是否能够协同工作。
- 排查早期生成结果中的白屏问题。
- 观察模型是否开始学习文本到图像分布的映射，尤其是颜色语义。

## 2. 实验设置

### 2.1 模型结构

- 文本底座：`Qwen3.5-9B`
- 图像表示：离散视觉 token
- 视觉 tokenizer：真实 `VQ-VAE / VQ-GAN`
- 生成方式：自回归预测图像 token
- 图像恢复：VQ decoder 将视觉 token 解码为 RGB 图像
- 条件方式：文本编码结果作为视觉解码器条件输入

### 2.2 数据规模

- 实际使用数据：约 `826` 张图文对
- 数据特征：规模极小，仅足以验证架构，不足以训练真正可用的文生图模型

### 2.3 实验重点

本实验不关注最终画质，不使用该结果证明模型“已经学会文生图”。重点在于：

- 工程链路闭环
- 训练稳定性
- 白屏根因定位
- 初步语义对齐信号

## 3. 工程验证结果

实验已经打通以下关键环节：

- 图文数据整理与 manifest 构建
- 真实视觉 tokenizer 编码
- 训练数据装载与 token 序列构造
- 条件自回归训练
- 文本条件采样
- VQ token 解码出图

这说明该架构不是停留在理论层面，而是已经具备可运行的工程基础。

## 4. 白屏问题排查

### 4.1 现象

训练早期生成结果大量表现为：

- 纯白图
- 浅色背景块
- 大面积高亮区域

### 4.2 排查结论

白屏问题不是图像保存错误，也不是 VQ decoder 失效，而是训练中 token 分布塌缩导致的结果。具体表现为：

- 模型在生成阶段反复输出少量高频浅色 token
- 整张图被极少数 token 主导
- 解码后自然表现为白屏或浅色块

因此，问题本质是训练分布退化，而不是推理流程 bug。

## 5. 针对塌缩的修正

为缓解高频 token 垄断和白屏问题，训练中加入了以下约束：

- 类别平衡 focal cross entropy
- token 分布 KL 约束
- 重复 token 抑制项
- 基于颜色统计的 token 权重
- blank / 浅色 token 抑制项

这些改动带来的直接效果是：

- 生成结果不再稳定塌缩成纯白图
- 输出从“全白”转为“带颜色结构的块状图”
- 颜色词对结果开始产生明显影响

这说明训练目标是可塑的，模型并非完全失效。

## 6. 语义识别能力分析

### 6.1 是否具备基本语义识别

结论：**具备初步、低层级的语义识别能力。**

这里的“基本语义识别”不是指模型已经能够稳定生成复杂对象，而是指某些文本词已经可以系统性地改变输出图像的统计特征。

### 6.2 颜色词响应

从生成结果看，`red`、`yellow`、`green` 等颜色词已经能够影响图像中的主色块分布。这意味着模型已经开始建立以下映射：

- 文本 token
- 条件表示
- 图像 token 分布

也就是说，它已经不再是纯随机生成，而是学到了最低层级的文本条件控制。

### 6.3 为什么先学会颜色

在极小数据集上，模型通常按如下顺序学习：

1. 高频背景与常见 token
2. 颜色这类低维、强统计信号
3. 局部部件
4. 全局轮廓
5. 多属性组合关系

因此颜色词先出现响应，而对象结构尚未成形，是符合训练规律的。

## 7. 为什么还不能算真正的文生图

虽然颜色条件已经开始起作用，但当前结果仍然不能说明模型已经学会稳定文生图，原因如下：

- 数据量过小，`826` 张图远不足以支持 9B 级模型学习复杂视觉语义
- 自回归视觉 token 预测在小数据上极易发生重复和塌缩
- 颜色语义只要求改变分布，而结构语义要求模型学会部件、空间关系和组合约束

因此，目前模型只能说明“开始理解颜色”，不能说明“已经理解对象”。

## 8. 结论

本实验可以得出以下结论：

1. `Qwen3.5-9B + 离散视觉 token + 自回归视觉解码` 这条路线在工程上是可跑通的。
2. 白屏问题的根因是训练中的 token 分布塌缩，而不是解码或保存错误。
3. 通过增加针对塌缩的损失项，可以明显缓解纯白生成。
4. 模型已经对 `red`、`yellow`、`green` 等颜色词表现出初步条件响应。
5. 这说明该系统已经开始学习文本到图像分布的映射，但还未达到稳定对象生成阶段。

## 9. 最终判断

如果把目标定义为“训出可用的文生图模型”，这次实验显然还没有完成。  
如果把目标定义为“验证架构是否真实可行，并确认模型是否已经出现基础语义对齐信号”，那么这次实验是成功的。

更准确地说，本次实验证明了：

**这套自回归文生图系统并不是空转，它已经在极小数据条件下学到了最低层级的颜色语义控制能力。**

这也是后续继续扩数据、延长训练、增强结构损失的前提。
 # 基于 Qwen3.5-9B 的自回归文生图架构验证报告

  ## 1. 研究目的

  本实验的目标不是训练出可用的高质量文生图模型，而是验证一套基于 `Qwen3.5-9B + 离散视觉 Tokenizer + 自回归视觉解码器` 的
  工程架构是否能够真正跑通，并初步观察模型是否已经具备最基础的文本语义对齐能力。

  重点验证的问题有两类：

  1. 工程链路是否闭环
     包括数据整理、视觉 tokenizer 编码、文本条件输入、训练、推理采样、VQ 解码出图等关键环节。
  2. 模型是否出现初步语义识别
     特别关注颜色词如 `red`、`yellow`、`green` 是否已经对输出图像产生可观测影响。

  ---

  ## 2. 实验背景与限制

  本次实验使用的数据规模非常小，仅约 `826` 张图文对。
  这一规模远低于训练真正文生图模型所需的数据量，因此本实验结论不能用于证明模型具备完整文生图能力，只能用于验证：

  - 架构设计是否正确
  - 训练目标是否可优化
  - 模型是否开始学习最低层级的文本条件信息

  也就是说，这更接近一次“体系结构可行性实验”，而不是一次“模型效果评测”。

  ---

  ## 3. 模型与系统结构

  本实验采用的核心结构如下：

  - 文本底座：`Qwen3.5-9B`
  - 图像表示：离散视觉 token
  - 视觉 tokenizer：真实 `VQ-VAE / VQ-GAN` 路线，而非 mock 数据
  - 生成方式：自回归预测图像 token
  - 解码方式：将生成出的视觉 token 通过 VQ decoder 还原成图像
  - 条件注入方式：文本编码结果作为视觉解码器条件输入

  整体上，实验完成了从文本输入到图像输出的完整闭环，说明该架构在工程上是可运行的。

  ---

  ## 4. 训练过程中发现的关键问题

  ### 4.1 白屏问题

  训练初期最明显的问题是生成结果接近纯白图或高亮背景块。
  后续排查表明，这一问题并不是图像保存流程错误，也不是 VQ 解码器损坏，而是训练后模型在 token 预测阶段发生了明显的分布塌
  缩：

  - 某些高频浅色 token 被反复预测
  - 整张图被单一 token 或极少数 token 主导
  - 解码后表现为白屏、浅色块、背景纹理

  这说明问题本质属于训练分布退化，而不是推理脚本 bug。

  ### 4.2 多样性不足

  虽然加入修正损失后，模型不再稳定输出纯白图，但生成结果依然主要停留在：

  - 色块
  - 条纹
  - 几何噪声
  - 粗糙结构

  这说明模型已经脱离“完全崩坏”状态，但还远未形成稳定的对象结构建模能力。

  ---

  ## 5. 针对塌缩问题采取的修正

  为抑制白屏和高频 token 垄断，本实验在训练中引入了若干额外约束，包括：

  - 类别平衡 focal cross entropy
  - token 分布 KL 约束
  - 重复 token 抑制项
  - 基于颜色统计的 token 权重
  - blank / 浅色 token 抑制项

  这些改动的直接效果是：

  - 纯白塌缩得到明显缓解
  - 输出图从“全白”变为“有颜色结构的块状图”
  - 模型开始对颜色类条件词产生响应

  虽然这些改动没有让模型学会稳定生成可辨识角色，但它们成功证明了训练目标可以被进一步塑形，而不是完全失效。

  ---

  ## 6. 语义识别能力分析

  ### 6.1 是否具备基本语义识别

  结论是：**具备初步、低层级的语义识别能力。**

  这里的“基本语义识别”并不是指模型已经理解复杂提示词，也不是指它能够稳定画出符合描述的完整对象，而是指：

  - 某些文本词汇已经能系统性地影响生成分布
  - 其中最明显的是颜色词

  ### 6.2 对颜色词的响应

  从实验结果看，`red`、`yellow`、`green` 等颜色词已经能够影响输出图像的主色块倾向。
  这意味着模型并非完全盲生成，而是已经开始建立：

  - 文本 token
  - 条件表示
  - 图像 token 分布

  之间的映射关系。

  换句话说，它已经开始学习“颜色语义 -> 图像统计特征”的对应。

  ### 6.3 为什么这很重要

  对于超小数据集训练而言，模型通常按以下顺序学习：

  1. 高频背景与常见 token
  2. 简单、低维、强统计信号的属性词
     例如颜色词
  3. 局部部件
  4. 全局形状
  5. 复杂组合语义

  因此，颜色词先于结构语义出现，是符合训练规律的。
  这恰恰说明当前架构不是完全失效，而是在数据极小的情况下，已经学到了最容易被捕捉的一层语义条件。

  ---

  ## 7. 为什么还没有学会“真正的文生图”

  虽然颜色词已经产生作用，但模型仍然无法稳定生成具有明确对象轮廓的宝可梦形象，原因主要有以下几点：

  ### 7.1 数据规模远远不够

  `826` 张图像只能用于验证链路，远远不够支持 9B 级语言底座学习复杂视觉组合规律。

  ### 7.2 视觉 token 学习难度高

  自回归方式需要逐步预测整串视觉 token。
  在小数据上，这类任务极易出现：

  - 高频 token 过拟合
  - 局部模式重复
  - 结构无法闭合

  ### 7.3 结构语义比颜色语义更难学

  颜色词只需要改变整体色彩分布，而“绿色宝可梦、红眼睛、乌龟炮管”这类描述则要求模型同时学会：

  - 局部属性绑定
  - 对象轮廓
  - 多属性组合
  - 视觉空间结构

  这些都不是当前数据规模能支撑的。

  ---

  ## 8. 本次实验的核心结论

  本实验可以得出以下结论：

  1. `Qwen3.5-9B + 离散视觉 token + 自回归视觉解码` 这条技术路线在工程上是可跑通的。
  2. 白屏问题的本质是训练中的 token 分布塌缩，而不是解码或保存流程错误。
  3. 通过增加针对 token collapse 的损失项，可以显著缓解纯白生成问题。
  4. 在极小数据集条件下，模型已经表现出最初级的文本语义对齐能力，尤其体现在颜色词 `red`、`yellow`、`green` 对输出颜色分
  布的影响上。
  5. 当前结果不能证明模型已经具备完整文生图能力，但足以证明该架构不是空转，已经开始学习文本到图像分布的映射。

  ---

  ## 9. 对后续工作的建议

  如果继续推进该项目，建议分三步进行：

  ### 9.1 先做可量化语义评估

  对固定 prompt 集合进行自动采样，例如：

  - `a red pokemon`
  - `a yellow pokemon`
  - `a green pokemon`

  统计生成图像的主色分布、饱和度和颜色占比，建立一个最基础的自动语义验证指标，避免只靠肉眼判断。

  ### 9.2 扩大数据规模

  若希望从“颜色响应”提升到“对象生成”，必须显著扩大训练数据量。
  当前 `826` 张数据只能做架构验证，不能支撑真正的文生图学习。

  ### 9.3 继续优化训练目标

  - 更强的重复 token 抑制
  - 更细粒度的颜色与结构联合损失


  尤其是颜色词 `red`、`yellow`、`green` 的响应，说明模型已经不再是纯随机生成，而是在极其有限的数据条件下学到了最初级的文
  本条件控制能力。

  对于一个仅使用 `826` 张图像的小规模实验来说，这个结果是成立且有研究价值的。